export enum userStatus {
    ACTIVE = "Active",
    INACTIVE = "Inactive"
}

export enum workRole {
    ADMIN = "Admin",
    ANALYST = "Analyst",
    VIEWER = "Viewer"
}

export enum messageSender {
    AI = "Ai",
    USER = "User"
}

